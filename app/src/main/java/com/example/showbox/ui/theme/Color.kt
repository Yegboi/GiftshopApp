package com.example.showbox.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * The single yellow in the app. It marks what is interactive or important —
 * buttons, sliders, the countdown, the alarm. Nothing else is yellow.
 */
val Yellow = Color(0xFFFFC400)

val Ink = Color(0xFF0D0D0D)
val InkRaised = Color(0xFF161616)
val InkVariant = Color(0xFF1E1E1E)
val InkOutline = Color(0xFF3A3A3A)

val TextWhite = Color(0xFFFFFFFF)
val TextMuted = Color(0xFFB2B2B2)

val OnYellow = Color(0xFF1A1400)
val Danger = Color(0xFFFF6B6B)
val OnDanger = Color(0xFF2A0000)

/** One colour per bottom-bar section, so the tabs are told apart at a glance. */
val TabShift = Yellow
val TabMusic = Color(0xFF4FC3F7)
val TabQuiz = Color(0xFFB388FF)
val TabDating = Color(0xFFFF7597)
val TabGuess = Color(0xFF4DD0A1)
val TabPodcast = Color(0xFFFFA24D)
